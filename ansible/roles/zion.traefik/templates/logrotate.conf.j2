{{ traefik_logs_folder }}/*.log {
  size 10M
  rotate 5
  missingok
  notifempty
  compress
  dateext
  dateformat .%Y-%m-%d
  create 0644 root root
  postrotate
    docker-compose kill -s USR1 traefik
  endscript
}
